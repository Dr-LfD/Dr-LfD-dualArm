sg_params:  
  h5_parent_dir: "/ssd1/aloha_data/handoff_cup"
  skill_name: 'handoff_cup'
  # skill_names: ['bimanual_0', 'grasp']
  skill_type: 'bimanualOr'
  pre_obj_names: ['cup']
  pre_arms: ['right']
  eff_obj_names: ['cup']
  eff_arms: ['left']
  output_dir: 'hdf5_out/handoff_cup'

# NOTE: x_threashold is revised for aloha2
  left:
    grasp:
      x_threshold: -0.8
      dx_threshold: -0.7
    release:
      x_threshold: -1.0
      dx_threshold: 0.5
      clip_start: 100

  right:
    grasp:
      x_threshold: -1.0
      dx_threshold: -0.7
    release:
      x_threshold: -1.2
      dx_threshold: 0.5
      clip_start: 100

  hand_obj_dist_threshold: 0.12
  hand_hand_dist_threshold: 0.18
  lifted_threshold: 0.15
  holding_truncation: 10

LfD_params:
  lfd_alg: 'DP'
  env_type: 'real'
  DP_path: '/home/xuhang/Desktop/yzchen_ws/Diffusion-Policy/'
  DP_input: 
    handoff_cup: '/ssd1/chenyizhou/dp_ckpts/handoff_cup/20demos/latest_diverse.ckpt'
    # handoff_cup: /'/ssd1/chenyizhou/dp_ckpts/handoff_cup/20demos/latest.ckpt'

